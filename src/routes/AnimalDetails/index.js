import React, { Component } from 'react'
import AnimalInfo from './components/AnimalInfo.js'

class AnimalDetails extends Component {
  render () {
    return (
      <div>
        <AnimalInfo />
      </div>
    )
  }
}

export default AnimalDetails
